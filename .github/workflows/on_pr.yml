name: PR checks

on:
  pull_request:
    branches: [master]

jobs:
  lint:
    name: Lint
    uses: ./.github/workflows/do_lint.yml

  test:
    name: Test
    uses: ./.github/workflows/do_test.yml

  generate:
    name: Generate
    uses: ./.github/workflows/do_generate.yml
